/**
 * Perform a single forward/backward pass of FABRIK, and change
 * the point locations in place
 * @param {list} Ps List of joints on the arm, passed by reference
 * @param {glMatrix.vec3} target Target point
 */
function FABRIKIter(Ps, target) {
    // TODO: Fill this in
}